[project]
name = "bb-skills"
version = "1.1.1"
description = "Install and manage BuildBetter Skills — AI coding skills enriched with BuildBetter customer evidence."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "BuildBetter", email = "eng@buildbetter.app" },
]
keywords = ["ai", "skills", "claude", "codex", "buildbetter", "spec-driven"]
dependencies = [
    "typer>=0.9,<1.0",
    "rich>=13.0,<14.0",
    "httpx>=0.24,<1.0",
    "pyyaml>=6.0,<7.0",
]

[project.urls]
Homepage = "https://github.com/buildbetter-app/skills"
Repository = "https://github.com/buildbetter-app/skills"
Issues = "https://github.com/buildbetter-app/skills/issues"

[project.scripts]
bb-skills = "bb_skills_cli:main"

[project.optional-dependencies]
test = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bb_skills_cli", "bb_skills_adapters", "skills", "scripts", "templates"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

[tool.coverage.run]
source = ["src", "bb_skills_adapters"]
omit = ["*/tests/*", "*/__pycache__/*"]
